AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Private ECR repository and a least-privilege IAM role that GitHub Actions
  assumes via OIDC to build and push container images. No long-lived AWS
  credentials are used.

Parameters:
  GitHubOrg:
    Type: String
    Description: GitHub organization or username that owns the repository.
  GitHubRepo:
    Type: String
    Description: Name of the GitHub repository allowed to assume this role.
  GitHubOrgId:
    Type: String
    Description: >
      Numeric, immutable GitHub owner ID (from GET /repos/{org}/{repo} ->
      .owner.id). GitHub's OIDC token embeds this as repo:ORG@ID/... once an
      account has ever been renamed, so the trust condition pins to the ID
      instead of the mutable name.
  GitHubRepoId:
    Type: String
    Description: >
      Numeric, immutable GitHub repository ID (from GET /repos/{org}/{repo}
      -> .id), used the same way as GitHubOrgId.
  EcrRepositoryName:
    Type: String
    Description: Name of the private ECR repository to create.
    Default: node-api
  Owner:
    Type: String
    Description: Owner tag applied to created resources.

Resources:
  EcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref EcrRepositoryName
      ImageTagMutability: MUTABLE
      ImageScanningConfiguration:
        ScanOnPush: true
      EncryptionConfiguration:
        EncryptionType: AES256
      RepositoryPolicyText:
        Version: '2012-10-17'
        Statement:
          - Sid: AllowGitHubActionsPushOnly
            Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${AWS::AccountId}:role/github-actions-ecr-push'
            Action:
              - ecr:BatchCheckLayerAvailability
              - ecr:GetDownloadUrlForLayer
              - ecr:BatchGetImage
              - ecr:PutImage
              - ecr:InitiateLayerUpload
              - ecr:UploadLayerPart
              - ecr:CompleteLayerUpload
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Expire untagged images after 7 days",
                "selection": {
                  "tagStatus": "untagged",
                  "countType": "sinceImagePushed",
                  "countUnit": "days",
                  "countNumber": 7
                },
                "action": { "type": "expire" }
              },
              {
                "rulePriority": 2,
                "description": "Keep only the last 20 tagged images",
                "selection": {
                  "tagStatus": "tagged",
                  "tagPrefixList": ["kevineiradukunda"],
                  "countType": "imageCountMoreThan",
                  "countNumber": 20
                },
                "action": { "type": "expire" }
              }
            ]
          }
      Tags:
        - Key: Project
          Value: docker-ecr-oidc-lab
        - Key: Owner
          Value: !Ref Owner
        - Key: ManagedBy
          Value: CloudFormation

  GitHubActionsEcrPushRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: github-actions-ecr-push
      Description: Assumed by GitHub Actions via OIDC to push images to a single ECR repository.
      MaxSessionDuration: 3600
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Federated: !Sub 'arn:aws:iam::${AWS::AccountId}:oidc-provider/token.actions.githubusercontent.com'
            Action: sts:AssumeRoleWithWebIdentity
            Condition:
              StringEquals:
                'token.actions.githubusercontent.com:aud': 'sts.amazonaws.com'
              StringLike:
                'token.actions.githubusercontent.com:sub': !Sub 'repo:${GitHubOrg}@${GitHubOrgId}/${GitHubRepo}@${GitHubRepoId}:*'
      Policies:
        - PolicyName: EcrPushToSingleRepository
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: EcrAuthToken
                Effect: Allow
                Action: ecr:GetAuthorizationToken
                Resource: '*'
              - Sid: EcrPushPullSingleRepo
                Effect: Allow
                Action:
                  - ecr:BatchCheckLayerAvailability
                  - ecr:GetDownloadUrlForLayer
                  - ecr:BatchGetImage
                  - ecr:PutImage
                  - ecr:InitiateLayerUpload
                  - ecr:UploadLayerPart
                  - ecr:CompleteLayerUpload
                Resource: !Sub 'arn:aws:ecr:${AWS::Region}:${AWS::AccountId}:repository/${EcrRepositoryName}'
      Tags:
        - Key: Project
          Value: docker-ecr-oidc-lab
        - Key: Owner
          Value: !Ref Owner
        - Key: ManagedBy
          Value: CloudFormation

Outputs:
  EcrRepositoryUri:
    Description: URI of the private ECR repository.
    Value: !Sub '${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com/${EcrRepository}'
  EcrRepositoryArn:
    Value: !GetAtt EcrRepository.Arn
  GitHubActionsRoleArn:
    Description: IAM role ARN for GitHub Actions to assume via OIDC.
    Value: !GetAtt GitHubActionsEcrPushRole.Arn
